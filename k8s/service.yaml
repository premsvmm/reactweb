apiVersion: v1
kind: Service
metadata:
  labels:
    app: reactweb
  name: reactweb
  namespace: default
spec:
  externalTrafficPolicy: Cluster
  ports:
    - nodePort: 30205
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: reactweb
  type: LoadBalancer